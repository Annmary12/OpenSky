import { flightsContants, IS_REQUESTING } from '../../constants/types';

export const getDepartingFlights = () => async (dispatch, getState, http) => {
  dispatch(Request(IS_REQUESTING, true));

  try {

  } catch (error) {

  }
}